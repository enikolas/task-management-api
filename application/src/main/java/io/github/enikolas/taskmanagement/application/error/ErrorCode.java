package io.github.enikolas.taskmanagement.application.error;

public enum ErrorCode {
    NOT_FOUND,
    DUPLICATED,
    INVALID_ARGUMENT,
    AUTHENTICATION_INVALID,
    AUTH_TOKEN_GENERATION_FAILURE
}
